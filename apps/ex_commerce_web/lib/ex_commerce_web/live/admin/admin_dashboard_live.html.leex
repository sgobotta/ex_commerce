<%= if connected?(@socket) do %>

  <%= render_layout(
        ExCommerceWeb.DashboardView,
        "dashboard",
        [current_page: "home", user_email: @user.email]
      )
  do %>

    <div>Home</div>

  <% end %>

<% end %>
